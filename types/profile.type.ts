export type Profile = {
  nama: string;
  email: string;
  total_transaction: number;
};

export type ProfileDetail = {
  nama: string;
  email: string;
  no_telpon: string;
  jenis_kelamin: string;
  tanggal_lahir: string;
};
